services:
  navidrome-discord:
    build: .
    image: navidrome-discord:latest
    container_name: navidrome-discord
    restart: unless-stopped
    env_file: .env
    environment:
      STATE_FILE: /data/state.json
    volumes:
      - botdata:/data

volumes:
  botdata: